#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")"

PYTHON_BIN="${OFFERGRAPH_PYTHON:-./.venv/bin/python}"
if [[ ! -x "$PYTHON_BIN" ]]; then
  PYTHON_BIN="${OFFERGRAPH_PYTHON:-python3}"
fi

exec "$PYTHON_BIN" scripts/agent_console.py "$@"
